import React, { Component } from "react";

import Layout from "../../components/Layout";

export default class Home extends Component {
  public render() {
    return (
      <Layout title="Full Command">
        Bem vindo ao full command, este projeto está em construção.
      </Layout>
    );
  }
}
